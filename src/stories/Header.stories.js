import { fn } from '@storybook/test';
import Header from './Header.vue';

export default {
  title: 'Example/Header',
  component: Header,
  tags: ['autodocs'],
};

export const Default = {
  args: {
    
  },
};
