import { fn } from '@storybook/test';
import MyPage from './MyPage.vue';

export default {
  title: 'Example/MyPage',
  component: MyPage,
  tags: ['autodocs'],
  argTypes: {
    
  },
  // args: { onClick: fn() },
};

export const Default = {
  
};
